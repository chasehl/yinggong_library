/* pages/me/me.wxss */
page {
  background-color: #f6f7f9;
}

/* 🔴 核心修复：强制占满屏幕，去除所有默认内边距 */
.container {
  padding: 0;
  width: 100%;
  box-sizing: border-box;
  overflow-x: hidden; /* 防止横向滚动条 */
}

/* --- 1. 顶部背景 --- */
.header-bg {
  height: 340rpx; /*稍微加高一点，比例更好*/
  background: linear-gradient(180deg, #4a6fa5, #5b7bb2); /* 加一点微渐变更高级 */
  border-bottom-left-radius: 40rpx;
  border-bottom-right-radius: 40rpx;
  width: 100%;
  position: absolute;
  top: 0;
  left: 0;
  z-index: 1;
}

/* --- 2. 用户卡片 --- */
.user-card {
  position: relative;
  z-index: 2;
  
  /* 🔴 修复：减小左右边距，从 30rpx -> 20rpx，卡片变宽 */
  margin: 150rpx 20rpx 24rpx; 
  
  background: #fff;
  border-radius: 20rpx;
  
  /* 🔴 修复：减小内部边距，让内容更靠边，视觉更宽敞 */
  padding: 30rpx 24rpx; 
  
  box-shadow: 0 8rpx 20rpx rgba(74, 111, 165, 0.1);
}

.user-info-row {
  display: flex;
  align-items: center;
  padding: 0 10rpx; /*微调内部对齐*/
}

/* 头像区域 */
.avatar-wrap {
  position: relative;
  margin-right: 24rpx; /* 减小间距 */
}

.avatar {
  width: 110rpx; /* 稍微调小一点头像，显得卡片更大 */
  height: 110rpx;
  border-radius: 50%;
  border: 4rpx solid #fff;
  box-shadow: 0 4rpx 10rpx rgba(0,0,0,0.1);
  background-color: #eee;
}

.edit-icon {
  position: absolute;
  bottom: 0;
  right: 0;
  background: rgba(0,0,0,0.6);
  color: #fff;
  font-size: 18rpx;
  width: 32rpx;
  height: 32rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 文字区域 */
.info-text {
  flex: 1;
  display: flex;
  flex-direction: column;
}

.name-wrap {
  display: flex;
  align-items: center;
  margin-bottom: 8rpx;
}

.nickname {
  font-size: 34rpx; /* 字体大小适中 */
  font-weight: bold;
  color: #333;
}

.edit-pen {
  font-size: 28rpx;
  color: #999;
  margin-left: 10rpx;
  padding: 10rpx; /* 增加点击区域 */
}

.dept {
  font-size: 24rpx;
  color: #888;
}

.arrow-right {
  color: #ccc;
  font-size: 32rpx;
}

/* 数据概览 */
.stats-row {
  display: flex;
  justify-content: space-around;
  margin-top: 30rpx;
  padding-top: 24rpx;
  border-top: 1rpx solid #f8f8f8;
}

.stats-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  flex: 1; /* 均分宽度 */
}

.stats-item .num {
  font-size: 36rpx;
  font-weight: bold;
  color: #333;
  margin-bottom: 4rpx;
  font-family: Arial, sans-serif; /* 数字字体更紧凑 */
}

.stats-item .label {
  font-size: 22rpx;
  color: #999;
}

/* --- 3. 菜单列表 --- */
.menu-group {
  background: #fff;
  
  /* 🔴 修复：与上面的卡片对齐，使用较小的边距 */
  margin: 0 20rpx 24rpx;
  
  border-radius: 16rpx;
  padding: 0 20rpx; /* 减小内部左右padding */
}

.menu-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  height: 100rpx; /* 高度稍微增加，手指更容易点 */
  border-bottom: 1rpx solid #f9f9f9;
}

.menu-item.last, .menu-item:last-child {
  border-bottom: none;
}

/* 按钮样式重置 */
.btn-reset {
  background: transparent;
  padding: 0;
  margin: 0;
  width: 100% !important;
  font-weight: normal;
  border-radius: 0;
  line-height: inherit; /* 修复按钮文字垂直对齐 */
}
.btn-reset::after { border: none; }

.item-left {
  display: flex;
  align-items: center;
}

.menu-icon {
  width: 36rpx;
  height: 36rpx;
  margin-right: 20rpx;
}

.menu-icon-sys {
  margin-right: 20rpx;
  display: flex;
  align-items: center;
}

.item-left text {
  font-size: 28rpx;
  color: #333;
}

.arrow {
  color: #ddd; /* 颜色淡一点 */
  font-size: 24rpx;
}

/* --- 4. 退出登录 --- */
.logout-area {
  margin: 40rpx 20rpx 60rpx;
}

.logout-btn {
  background: #fff;
  color: #e74c3c;
  font-size: 30rpx;
  border-radius: 16rpx; /* 稍微方一点，显得正式 */
  height: 88rpx;
  line-height: 88rpx;
  box-shadow: 0 2rpx 6rpx rgba(0,0,0,0.03);
}